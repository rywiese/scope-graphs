use List.nth
use List.Hat.q
use List as LList
use Q.A.f as g

type alias Static = Internal.IO

enum List[t] {
    case Nil
    case Cons(t, List[t])
}

mod List {
    use Eq.eq
    use ToString.toString
    use Category.Functor

    type alias F[t, q] = Q.A.T[String, t[q]]

}

mod Q {
    mod Q2.Q3 {}
    mod A {

        type alias T = String
    }
}
