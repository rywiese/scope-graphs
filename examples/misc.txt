use List.nth
use List.Hat.q
use List as LList
use Q.A.f as g

pub type alias Static = Internal.IO

pub enum List[t] {
    pub case Nil
    pub case Cons(t, List[t])
}

pub mod List {
    pub use Eq.eq
    use ToString.toString
    use Category.Functor

    pub type alias F[t, q] = Q.A.T[String, t[q]]

}

mod Q {
    pub mod Q2.Q3 {}
    mod B.B {}
    pub mod A {

        type alias T = String
    }
}
